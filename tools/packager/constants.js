module.exports = {
  ROOT_PATH: require('path').join(__dirname, '..'),
  DIST_PATH: require('path').join(__dirname, '../dist'),
  INSTALLERS_PATH: require('path').join(__dirname, '../dist/installers')
}
